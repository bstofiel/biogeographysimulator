<p id="notice"><%= notice %></p>

<h1>Listing Islands</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Project</th>
      <th>Width</th>
      <th>Length</th>
      <th>Distance</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @islands.each do |island| %>
      <tr>
        <td><%= island.name %></td>
        <td><%= island.project_id %></td>
        <td><%= island.width %></td>
        <td><%= island.length %></td>
        <td><%= island.distance %></td>
        <td><%= link_to 'Show', island %></td>
        <td><%= link_to 'Edit', edit_island_path(island) %></td>
        <td><%= link_to 'Destroy', island, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Island', new_island_path %>
